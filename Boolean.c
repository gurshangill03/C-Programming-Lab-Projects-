#include <stdio.h>
#include <stdbool.h> //For boolean data type (bool,true,false)

//Main Function
int main(){
    //Boolean data types declared
    bool a=true; //'a' initialized to true
    bool b=false; //'b' initalized to false

    //printing boolean values as integers (true=1, false=0)
    printf("True: %d\n",a);
    printf("False: %d\n",b);

    return 0;
}